import SafetyCardWritePage from "@/components/safety-card-write/SafetyCardWritePage";

export const metadata = {
  title: "통합 작업계획서 작성 | PTW",
};

export default function Page() {
  return <SafetyCardWritePage />;
}
